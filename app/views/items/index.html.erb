<h6>
  <%= link_to "HOME", root_path %>
  <%= link_to "ADMIN", admin_path %>
</h6>

<h1>Items</h1>

<div id="items">
  <% @items.each do |item| %>
    <%= link_to "#{item.id}_#{item.language.name}_#{item.status}", item_path(item) %>
  <% end %>
</div>

<hr>
